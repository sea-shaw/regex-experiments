{
  final class $anon() extends Regex[Unit] {
    private[this] val pattern: Pattern = Pattern.compile("a")
    override def unapply(s: String): Option[Unit] = Option.when[Unit]($anon.this.pattern.matcher(s).matches())(())
  }

  (new $anon(): Regex[Unit])
}